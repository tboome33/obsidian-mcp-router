---
description: Read the full content of a file from a vault (markdown + frontmatter). (Skill `read-get` handles natural-language triggers + render formatting.)
---

Invoke the `read-get` skill.

Required: `path`. Optional: `vault`.
