---
description: Read frontmatter (the whole object or one key) from a file. Types are preserved. (Skill `read-frontmatter` handles natural-language triggers + render formatting.)
---

Invoke the `read-frontmatter` skill.

Required: `path`. Optional: `vault`, `key` (omit to get whole frontmatter).
