---
description: List files and subdirectories inside a vault directory (or vault root). (Skill `discover-list-files` handles natural-language triggers + argument parsing.)
---

Invoke the `discover-list-files` skill.

Optional: `vault` (default vault if omitted), `directory` (vault root if omitted).
