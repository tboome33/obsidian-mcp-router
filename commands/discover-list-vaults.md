---
description: List all configured Obsidian vaults — active (online/latency/metadata) and disabled (skipped). (Skill `discover-list-vaults` handles natural-language triggers + render adaptation.)
---

Invoke the `discover-list-vaults` skill.

Takes no arguments. The skill adapts the response based on whether the user asked about active vaults, disabled vaults, or both.
