---
description: Append content at the end of a file. Auto-creates the file unless `requireExisting=true`. Best for daily journals, logs, running captures. (Skill `write-append` handles natural-language triggers.)
---

Invoke the `write-append` skill.

Required: `path`, `content`. Optional: `vault`, `requireExisting` (default false — auto-creates the file).
