---
description: Plain-text (substring) search inside a vault, with surrounding context. (Skill `read-search` handles natural-language triggers + cross-vault fan-out via `vault=*`.)
---

Invoke the `read-search` skill.

Required: `query`. Optional: `vault` (default or `*` for fan-out), `contextLength` (default 100).
